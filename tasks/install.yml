---
- name: Create the idrac_exporter group
  group:
    name: "{{ idrac_exporter_system_group }}"
    state: present
    system: true

- name: Create the idrac_exporter user
  user:
    name: "{{ idrac_exporter_system_user }}"
    groups: "{{ idrac_exporter_system_group }}"
    append: true
    shell: /usr/sbin/nologin
    system: true
    createhome: false
    home: /
